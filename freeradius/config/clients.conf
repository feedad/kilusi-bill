# ===========================================
# FreeRADIUS Clients Configuration
# ===========================================
# ALL clients are loaded from database via SQL (read_clients = yes)
# No fixed clients - each NAS is identified by its real IP
# ===========================================